name: Auto-merge

on:
  pull_request_target:

permissions:
  pull-requests: write
  contents: write

jobs:
  merge-formula-pr:
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'ArnaudLigny' }}
    steps:
      - name: Auto-merge Formula PR
        run: gh pr merge --auto --merge "$PR_URL"
        env:
          PR_URL: ${{github.event.pull_request.html_url}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
